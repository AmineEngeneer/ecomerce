{
  "buildCommand": "node scripts/prepare-db.cjs && npx prisma generate && npx prisma db push --accept-data-loss && vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
